# MUSIC-TO-CLONE-HERO-FULL-BAND-AI

A lightweight website prototype for generating Clone Hero chart project packages from YouTube or Spotify links.

## Run locally

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
